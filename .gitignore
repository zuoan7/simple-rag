# 排除敏感的.env文件（必加！）
.env

# 排除向量库文件夹（关键：替换成你项目里向量库的实际名称，比如chroma_db/、vector_store/）
向量库文件夹名/

# 排除Python虚拟环境（你的环境是rag-env，对应排除）
rag-env/
venv/

# 排除Python自动生成的缓存文件
__pycache__/
*.pyc
*.pyo
*.pyd

# 排除Windows/Mac系统临时文件
.DS_Store
Thumbs.db
